import { useState } from 'react'
import { Link, useNavigate } from 'react-router-dom'
const departments = [{ id: 'dept-1', name: 'Road Maintenance — Zone 1' }, { id: 'dept-2', name: 'Road Maintenance — Zone 2' }, { id: 'drainage', name: 'Drainage & Waterlogging' }]
export default function LoginScreen() { const [department, setDepartment] = useState('dept-1'); const navigate = useNavigate(); return <main className="grid min-h-screen place-items-center bg-slate-950 p-5 text-slate-100"><form onSubmit={(e) => { e.preventDefault(); navigate(`/departments/${department}`) }} className="w-full max-w-md rounded-2xl border border-white/10 bg-slate-900 p-7 shadow-2xl"><Link to="/" className="text-xs font-bold text-cyan-400">← NAVBHARAT</Link><h1 className="mt-8 text-2xl font-semibold">Department login</h1><p className="mt-2 text-sm text-slate-400">Demo access to the assigned evidence queue.</p><label className="mt-7 block text-xs font-medium text-slate-300">Department</label><select value={department} onChange={(e) => setDepartment(e.target.value)} className="mt-2 w-full rounded-lg border border-white/10 bg-slate-800 p-3 text-sm">{departments.map((d) => <option value={d.id} key={d.id}>{d.name}</option>)}</select><label className="mt-5 block text-xs font-medium text-slate-300">Officer ID</label><input required defaultValue="OFF-104" className="mt-2 w-full rounded-lg border border-white/10 bg-slate-800 p-3 text-sm" /><button className="mt-6 w-full rounded-lg bg-cyan-400 p-3 text-sm font-bold text-slate-950">Open assigned queue</button><p className="mt-4 text-center text-xs text-slate-500">Admin dashboard is intentionally separate.</p></form></main> }
