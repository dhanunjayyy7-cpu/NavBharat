import { Link } from 'react-router-dom'
import { evidence } from '../lib/evidenceStore'
export default function AdminDashboard() { const items = evidence(); const stats = [['Reported potholes', items.length], ['Fixed', items.filter((x) => x.status === 'Fixed').length], ['Awaiting action', items.filter((x) => x.status !== 'Fixed').length], ['High severity', items.filter((x) => x.severity === 'High').length]]; return <main className="min-h-screen bg-slate-950 p-5 text-slate-100"><header className="mx-auto flex max-w-6xl items-center justify-between"><div><Link to="/" className="text-xs font-bold text-cyan-400">NAVBHARAT</Link><h1 className="mt-1 text-2xl font-semibold">Admin operations</h1></div><span className="text-xs text-slate-500">Separate endpoint · /admin</span></header><div className="mx-auto mt-10 max-w-6xl grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{stats.map(([label, number]) => <div key={label} className="rounded-xl border border-white/10 bg-slate-900 p-5"><p className="text-xs text-slate-400">{label}</p><p className="mt-3 text-3xl font-semibold text-cyan-300">{number}</p></div>)}</div><section className="mx-auto mt-8 max-w-6xl overflow-hidden rounded-xl border border-white/10"><div className="border-b border-white/10 p-5"><h2 className="font-semibold">Pothole action register</h2><p className="mt-1 text-xs text-slate-500">Reported time, assigned department, and current action.</p></div><div className="overflow-x-auto"><table className="w-full min-w-[650px] text-left text-sm"><thead className="bg-white/[.03] text-xs text-slate-500"><tr><th className="p-4">Report</th><th>Department</th><th>Report time</th><th>Action</th><th>Coordinates</th></tr></thead><tbody>{items.map((x) => <tr key={x.id} className="border-t border-white/5"><td className="p-4"><p>{x.id}</p><p className="text-xs text-slate-500">{x.location}</p></td><td>{x.department}</td><td>{x.date}</td><td><span className="rounded-full bg-cyan-400/10 px-2 py-1 text-xs text-cyan-300">{x.status}</span></td><td className="text-slate-400">{x.coordinates}</td></tr>)}</tbody></table></div></section></main> }
